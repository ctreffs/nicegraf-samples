#include <metal_stdlib>
using namespace metal;

struct Outputs {
  float4 pos [[position]];
  float4 color;
};

fragment float4 main0(Outputs o [[stage_in]]) {
  return float4(0.5 * (o.color.xyz + 0.5), 1.0);
}
